## Redirects for Refinery CMS

Enables the ability to manage multiple redirects in a new list.


## Installation
Simply update your ``Gemfile`` and add:

```ruby
gem 'refinerycms-redirects'
```

Then bundle:

    bundle install
    rake db:migrate
    rake db:seed